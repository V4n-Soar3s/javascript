// Também consigo arredondar se necessário o random entre 10 e 5, exemplo: 

const aleatorio = Math.round(Math.random( ) * (10 - 5) + 5);// adicionamos os parenteses e o Math.round antes da declaração fazendo que a mesma vire um número inteiro mais proximo 
console.log(aleatorio);