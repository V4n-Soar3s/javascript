//Também há a função shift que remove do começo do array, exemplo:

const alunos = ['Luiz', 'Ana', 'Maria'];

const removido = alunos.shift( );//removi um elemento do começo
console.log(removido); //criei uma variavel para salvar o elemento removido 
console.log(alunos); // solicitei que seja exibida a minha lista após a remoção do elemento 